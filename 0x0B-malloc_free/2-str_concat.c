#include "main.h"
#include <stdio.h>

void concatenate_strings(char str1[], char str2[]);
{
strcat(str1, str2);
}

int main(void)
{
char str1[100] = "Hello, ";
char str2[] = "world!";

concatenate_strings(str1, str2);

printf("%s\n", str1);

return (conct);

}

